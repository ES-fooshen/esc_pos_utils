name: discover_printers
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  charset_converter: ^2.0.0
  esc_pos_utils:
    path: ../../
  esc_pos_printer:
    git:
        url: https://github.com/ES-fooshen/esc_pos_printer
        ref: master
  flutter:
    sdk: flutter

dependency_overrides:
  esc_pos_utils:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
